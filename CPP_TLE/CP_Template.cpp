#include <bits/stdc++.h>
using namespace std;

#define endl '\n'               //convert endl to \n
#define int long long           //convert int to long long int

const int MOD = 1e9 + 7;         // 10^9 + 7
const int INF = LLONG_MAX >>1;   // means infinity = 4 X 10^8 . In most problem you never crossed 1 X 10^8. so you can compare with it.  
                                // >>1 means divided by 2.
signed main(){                  // if i write int main then it will be converted into long long. signed will work as int and code start from int main
    ios::sync_with_stdio(false);    //Remove sync between cout and printf.
    cin.tie(NULL);                  //Remove sync between cout and cin.

    int tc;                         //tc means test cases
    cin>>tc;

    while(tc--){                    //Repeat for tc number of times 

    }
}